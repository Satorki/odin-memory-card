const Card = ({ charname }) => {

  return (
    <>
      {/* <img
        className="w-40 rounded-xl"
        src={charname.images[0]}
        alt={charname.name}
      /> */}
    </>
  );
};

export default Card;
